
        class ABC extends Character {
            int a, b,c;
            float foo(int a; float c, d) {
            int e ;
            return (a<=c).name + 1;
            }
            float goo (float a, b) {
            return x.foo(1, a, b);
            }
            void main(){
                if ((a > 0) || (b >0)) then
                    io.writeLn("My name is " + this.name);
                else break;
            }
        }
        