class X{
            int k;
            static void main(){
                io.writeStr("hippo");
                X m;
                m.k=5;
                io.writeBool(this.notequal(m));
            }
            boolean notequal(X source){
                if this.k==source.k+1 then return false;
                else return true;
            }
            }